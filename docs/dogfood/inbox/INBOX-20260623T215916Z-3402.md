<!-- dogfood inbox draft: sev=sev2 area=git -->
# [dogfood] loop-arch 02: test-gate: fast model runs lint/test in worktree to pass/fail

## Problem
Step 8. After the parallel build, a small fast model enters each worktree, runs lint and tests, and emits an objective PASS/FAIL. Verdict is factual (model never fixes code) and is carried into the review/election round and used as the branch condition for the recovery ladder. Reuses detect_verify() in je-git.sh.

## Durable evidence (verbatim excerpt)
Step 8. After the parallel build, a small fast model enters each worktree, runs lint and tests, and emits an objective PASS/FAIL. Verdict is factual (model never fixes code) and is carried into the review/election round and used as the branch condition for the recovery ladder. Reuses detect_verify() in je-git.sh.

See docs/design/loop-architecture.md for the full pipeline, invariants, and pattern mapping.

## Repro
_(none provided)_

## Suspected fix
_(none provided)_

---
_Provenance (non-durable breadcrumb): run-id `unknown`. `.runs/` may be gc'd — evidence above is the durable record._
