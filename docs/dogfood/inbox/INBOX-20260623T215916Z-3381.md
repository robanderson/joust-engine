<!-- dogfood inbox draft: sev=sev2 area=git -->
# [dogfood] loop-arch 01: output security gate before implementer applies winner

## Problem
Step 10 of the loop. Before joust-implementer applies a winning proposal to a real branch, scan it for destructive ops (rm -rf, force-push, mass deletion), leaked secrets, and out-of-scope writes. Fail-closed to human review on a hit. Complements existing input/env-side verify gates in je-git.sh with an output-side gate.

## Durable evidence (verbatim excerpt)
Step 10 of the loop. Before joust-implementer applies a winning proposal to a real branch, scan it for destructive ops (rm -rf, force-push, mass deletion), leaked secrets, and out-of-scope writes. Fail-closed to human review on a hit. Complements existing input/env-side verify gates in je-git.sh with an output-side gate.

See docs/design/loop-architecture.md for the full pipeline, invariants, and pattern mapping.

## Repro
_(none provided)_

## Suspected fix
_(none provided)_

---
_Provenance (non-durable breadcrumb): run-id `unknown`. `.runs/` may be gc'd — evidence above is the durable record._
