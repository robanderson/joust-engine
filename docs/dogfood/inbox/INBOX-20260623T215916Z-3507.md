<!-- dogfood inbox draft: sev=sev2 area=skill -->
# [dogfood] loop-arch 07: recovery ladder when all candidates fail

## Problem
Step 9 recovery. When every candidate fails the test-gate/election: (a) cheap first resort re-build of the same plan with failure feedback; (b) escalate to a re-plan from step 3 with failure feedback. Lives in the Phase 7 grand-loop driver; replaces the current hard halt-on-failure.

## Durable evidence (verbatim excerpt)
Step 9 recovery. When every candidate fails the test-gate/election: (a) cheap first resort re-build of the same plan with failure feedback; (b) escalate to a re-plan from step 3 with failure feedback. Lives in the Phase 7 grand-loop driver; replaces the current hard halt-on-failure.

See docs/design/loop-architecture.md for the full pipeline, invariants, and pattern mapping.

## Repro
_(none provided)_

## Suspected fix
_(none provided)_

---
_Provenance (non-durable breadcrumb): run-id `unknown`. `.runs/` may be gc'd — evidence above is the durable record._
