<!-- dogfood inbox draft: sev=sev3 area=parse -->
# [dogfood] loop-arch 08: plan-only mode plus early loop exit

## Problem
Step 6 gate. A plan-only mode stops each loop after the winning plan is judged, emitting the plan (and PR) instead of building code. Signalled by its own grammar token, NOT by overloading Z (which keeps meaning number of grand loops).

## Durable evidence (verbatim excerpt)
Step 6 gate. A plan-only mode stops each loop after the winning plan is judged, emitting the plan (and PR) instead of building code. Signalled by its own grammar token, NOT by overloading Z (which keeps meaning number of grand loops).

See docs/design/loop-architecture.md for the full pipeline, invariants, and pattern mapping.

## Repro
_(none provided)_

## Suspected fix
_(none provided)_

---
_Provenance (non-durable breadcrumb): run-id `unknown`. `.runs/` may be gc'd — evidence above is the durable record._
